				    dlinsert
		insert dynamic libraries into existing binaries

This program is a fork of Tylio's "insert_dylib". I am in the process of
cleaning up and simplifying the code. So far, I have removed the dependency on
Xcode and opted to used CMake for builds.

For more information, see the original project:

<https://github.com/Tyilo/insert_dylib>
